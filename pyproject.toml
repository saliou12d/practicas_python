[tool.black]
line-length = 88
target-version = ["py313"]

[tool.isort]
profile = "black"
line_length = 88

[tool.flake8]
max-line-length = 88
extend-ignore = ["E203", "W503"]

[tool.bandit]
exclude = ["tests", "venv", ".venv"]
skips = ["B101"]  # puedes quitar esto si quieres que no ignore nada
